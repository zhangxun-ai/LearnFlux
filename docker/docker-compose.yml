# @generated by docker-package skill
# @skill-version 1
services:
  video-transcript-api:
    # Keep the service/container identifiers stable for existing deployments.
    image: ghcr.io/zhangxun-ai/learnflux:latest
    pull_policy: build
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: video-transcript-api
    environment:
      - TZ=Asia/Shanghai
    ports:
      # 宿主端口 8200（n305 上 portainer 已占 8000）；外部入口
      # https://sum.lexgogo.site 经 Cloudflare Tunnel 落到 :8200。
      - "8200:8000"
    volumes:
      # Configuration files (user must prepare before starting)
      - ../config:/app/config
      # Persistent data: cache, logs, transcription results
      - ../data:/app/data
    mem_limit: 2g
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
